To compile the program in debug mode run compiler.bat in the build folder path and to run the compiler in release mode use compiler_release.bat, 
in both cases the executable will be generated in the bin folder


Project by Lucas Calatayud and Carlos Mazcuñán